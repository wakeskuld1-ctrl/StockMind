# Stock Modeling Lifecycle Split Design

The modeling family is split into request-time online scoring and offline lifecycle governance. Online scoring owns live scored artifacts, while lifecycle owns registry, refit, training, and promotion.
